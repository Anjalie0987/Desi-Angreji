/**
 * Image utilities placeholder
 * Reusable functions for image transformations, generating blur data URLs, etc.
 */

export function getImageUrl(publicId: string) {
  // Placeholder implementation
  return `https://res.cloudinary.com/demo/image/upload/${publicId}`;
}
