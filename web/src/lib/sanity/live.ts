import { sanityFetch } from './fetch';

/**
 * Placeholder for Sanity Live Content API (Visual Editing).
 * The current next-sanity version does not support defineLive yet.
 */
export const liveSanityFetch = sanityFetch;

export function SanityLive() {
  return null;
}

